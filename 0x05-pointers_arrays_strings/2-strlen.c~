#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 *_strlen - function that returns the length of a string
 *
 *@s : is a parameter
 *
 *Return : length
 */

int _strlen(char *s)
{
int length;
length = strlen(*s);
}
